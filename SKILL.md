---
name: aigc-shed
description: 论文AIGC降重——系统性剥离AI生成痕迹，保留学术核心并注入人类写作特征。当用户提到降AIGC、降重、降AI、论文查重、AI查重、AIGC查重、AI检测、降低AI率、降低疑似率、降低疑似度、降低AI疑似率、降低重复率、论文改写、去AI痕迹、去机器味、去GPT味、去AI味、AIGC降重、规避查重、过查重、过AIGC检测、改写降重、降低查重率、知网查重、维普查重、万方查重、PaperPass、GPTZero、ZeroGPT、学术不端检测、学校查重，或提供了论文段落并要求"改写降重""降低查重率""去AI味""降低AI率""过学校查重"时触发。不适用于普通论文写作、文献综述生成、论文翻译、论文排版、论文润色（非AIGC降重目的）。
---

# 论文AIGC降重 v3（AIGC-Shed）

**目标**：AIGC 疑似度降至 15% 以下，极限模式目标 <10%。

## v3 新能力

1. **🧬 自进化**：每次使用后自动学习，策略随使用次数增长而优化
2. **🔗 多模型接力池**：用不同AI模型接力改写，彻底破坏指纹

## 启动时必做：读取进化数据

```
每次触发本技能时，先读取 evolution/patterns.json 和 evolution/evolution-log.md
了解当前最佳策略权重和最近的成功/失败案例。
```

## 核心认知

普通改写从 80% 到 49% 是瓶颈——GPTZero Paraphraser Shield 识别同义词替换，知网 BERT 看风格一致性。

**突破瓶颈**：破坏 8 个维度的机器指纹。本技能每段至少用 4 招同时攻击。

## 快速决策

```
用户输入
├── "帮我全文降AI" → 完整四阶段流程，加载进化数据
├── "这段标红/疑似AI" → 只改指定段落，深度模式
├── "帮我分析AI痕迹" → 仅诊断报告
├── "目标是过知网/GPTZero/..." → 加载检测器专项权重
├── "开启极限模式" → 多模型接力 + 全文对抗 + 回译链
├── "开启多模型模式" → 🆕 调用多模型接力池
├── "查看进化数据" → 读取 evolution/ 展示历史效果
└── 未指定 → 默认知网策略，取进化数据推荐权重
```

## 阶段零：进化学习 🆕

改写前必须执行：读取 `evolution/patterns.json`，获取当前最佳技法权重。

```
如果有针对目标检测器的专项权重 → 使用专项权重
如果无 → 使用默认权重
如果进化数据≥5条 → 分析趋势，调整策略
```

## 阶段一：源头判断

详见 `references/model-fingerprints.md`

```
GPT/DeepSeek格式化 → 句式大拆+结构重组
Claude长句 → 断长句+去自谦+加数据
DeepSeek虚词密 → 虚词瘦身+连词删减
通义/Gemini短句 → 合并短句+补逻辑链
```

## 阶段二：8维诊断（含 AI 模式扫描）

```
【第X段】N字
├── 句长CV: 0.XX (>0.5)       ← 不达标→句长随机化
├── 虚词密度: XX/千字 (<90)    ← 不达标→虚词瘦身
├── 模糊量化词: X个 (=0)       ← 全部具体化
├── 连词数: X个 (<2/200字)     ← 超标→删除
├── 句首重复: XXX              ← 有→多样化
├── 论证路径: 模板XXX           ← 标出→重构
├── 个人声音: 无/有            ← 无→注入
└── AI模式扫描: C1-C12/E1-E10  ← 详见 zh-ai-patterns.md
    检出模式: [如:C1过度意义, C4否定排比, E3高频词]
```

## 阶段三：7招必杀（每段≥4招）

### 必杀1：句长随机化（权重最高）
CV>0.5，不能连续3句长度相近。最长句拆2-3句，中间插超短句（3-8字）。

### 必杀2：虚词瘦身
密度<90/千字。删"的/了/是/在/和/与/及/等/其/此/该"。

### 必杀3：分句重构
30%无主语句 + 15%非常规语序 + 穿插口语化插入语。

### 必杀4：具体化轰炸
每200字≥1个具体数字或案例。所有模糊词→具体数值。

### 必杀5：论证路径差异化
轮换3种路径：A问题→数据→反问 / B案例→观点→疑问 / C现象→数据→评述

### 必杀6：注入个人声音
每3段≥1句个人评述。设问+不确定+批判。

### 必杀7：AI 模式清除 🔥（Humanizer 借鉴）
**不只是去痕迹，还要注入灵魂。** 改写同时做两件事：

**A. 清除模式（做减法）：**
对照 `references/zh-ai-patterns.md` 逐项检查：
- C1 过度意义 → 删大词，只留事实
- C2 虚假深度 → 把"体现/彰显"改成具体原因
- C4 否定排比 → "不仅…更…"改直述
- C5 三连排比 → 拆成单点叙述
- C6 同义反复 → 固定用同一词，不来回换
- C7 空洞结尾 → 改具体总结+不足
- C8 模糊归因 → 加具体文献出处
- C9 模板挑战 → 重构为个性化分析
- C11 句首模板 → 删掉模板句首直接说事
- C12 连词链条 → 删除结构性连词，靠内容过渡

**B. 注入灵魂（做加法）：** 每段至少完成 1 项：
- **有态度**：不只报告事实，要加判断（"这个发现值得警惕"而非"这个发现值得关注"）
- **有节奏**：长短句交错，让呼吸感出来
- **有矛盾**：承认复杂性（"效果很好但副作用也不小"）
- **有具体感受**：不要"效果显著"，要"看到数据时确实吃了一惊"
- **有毛边感**：1-2处口语化插入语（"说实话""坦白讲""但问题在于"）
- **丢一个反问**：每 2-3 段末尾丢一个问题

完整模式清单和示例见 `references/zh-ai-patterns.md`。

**C. 英文模式清除（英文论文用）：** 对照 `references/zh-ai-patterns.md` E1-E10 逐项检查：
- E1 Em dash 滥用 → 每段 ≤1 个，其余改逗号或句号
- E2 "Not only…but also…" 过度 → 改直述句
- E3 AI 高频词汇 → 替换（Additionally→Also, pivotal→key, underscore→show）
- E4 模糊归因 → 给具体文献出处
- E5 句首单一 → 多样化：时间状语开头/疑问句开头/条件句开头
- E6 开头陈词滥调 → 重写开头，用具体问题或具体场景切入
- E7 填充短语 → 直接删除
- E8 过度限定 → 直接说结论
- E9 假范围 "From X to Y" → 改为具体范围
- E10 过度干净 → 注入缩略语/反问句/不完整句

## 阶段四：全文对抗 + 自进化记录

1. 风格轮转：不同章节不同口吻
2. 合理不完美：1-2处的/地/得混用
3. 逻辑断点：1-2处留合理疑问
4. 引用加持：2-3处真实引用
5. **模式涤清**：全文过一次 C1-C12 / E1-E10 检查清单

**改写完成后必须更新进化数据！**

## 🔗 极限模式：多模型接力池 🆕

当用户要求极限模式或目标 <10% 时，启动多模型管线。

详见 `references/multi-model-pipeline.md`。

三种实现方式：
- **方式A**：平台自动编排（子代理/工作流调度不同模型接力）
- **方式B**：手动多模型（Claude→GPT→润色，手动跨平台操作）
- **方式C**：伪多模型（同一模型分3轮，每轮切换角色人设+清空上下文）

根据当前环境选择可用方式。

## 输出格式

```markdown
## 🔍 AI痕迹诊断
[逐段8维诊断 + AI模式扫描结果]

## ✏️ 改写结果
[改写后完整文本]

## 📊 改动统计
句式重构:X处 | 虚词删减:X处 | 具体化注入:X处 | 论证变化:X段 | 个人声音:X处 | 模式清除:X种

## ⚠️ 自检清单
- [ ] 全文句长CV>0.5
- [ ] 虚词密度<90/千字
- [ ] 每200字有具体数据
- [ ] 无AI高频词
- [ ] 段落间风格有差异
- [ ] 有个人声音和批判性
- [ ] C1-C12 模式已清除（中文适用）
- [ ] E1-E10 模式已清除（英文适用）

## 🧬 进化数据
改写前AIGC率: _请用户填写_
改写后AIGC率: _请用户检测后填写_
使用技法: [列出]
使用模式: 标准/极限/多模型
```

## 改写后：写入进化日志 🧬

改写结束后，在 `evolution/evolution-log.md` 追加一条记录：
```
日期 | 检测器 | 前% | 后% | 技法 | 模式 | 备注
```

如果用户提供了改写后AIGC率，同步更新 `evolution/patterns.json` 中的权重。

## 参考资源

- `references/model-fingerprints.md`：各AI模型写作指纹
- `references/detection-logic.md`：检测器原理与弱点
- `references/rewriting-techniques.md`：7大技法详解
- `references/term-mappings.md`：高频词替换表
- `references/bypass-arsenal.md`：逐检测器突破方案
- `references/multi-model-pipeline.md`：🆕 多模型接力管线
- **`references/zh-ai-patterns.md`**：🆕 中文+英文AI写作模式检测清单（C1-C12 + E1-E10，含Before/After示例）
- `evolution/`：🧬 进化引擎数据目录

## 参数

| 参数 | 选项 | 说明 |
|------|------|------|
| 目标检测器 | 知网/维普/万方/GPTZero/ZeroGPT/通用 | 加载专项策略权重 |
| 强度 | 轻度/中度/深度/极限 | 极限=多模型+回译链+全文对抗 |
| 目标AIGC率 | 默认<15% | <10%自动建议极限模式 |
| 术语保护 | 用户指定 | 不改动的术语列表 |
| 多模型 | 开启/关闭 | 启用多模型接力池改写 |
